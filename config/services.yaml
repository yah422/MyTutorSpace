# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    google_recaptcha_site_key: "%env(GOOGLE_RECAPTCHA_SITE_KEY)%"
    pdf_directory: "%kernel.project_dir%/public/uploads/pdfs"
    uploads_directory: "%kernel.project_dir%/public/uploads/photos"
    photos_directory: "%kernel.project_dir%/public/uploads/photos"
    avatars_directory: "%kernel.project_dir%/public/uploads/avatars"
    app.jwtsecret : '%env(JWT_SECRET)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: "../src/"
        exclude:
            - "../src/DependencyInjection/"
            # - '../src/Entity/'
            - "../src/Kernel.php"

    # Services Calendar explicitement définis
    # App\Services\Calendar\CalendarService:
    #     arguments:
    #         $entityManager: "@doctrine.orm.entity_manager"
    #         $availabilityRepository: '@App\Repository\TutorAvailabilityRepository'