{% extends 'base.html.twig' %}

{% block title %} Ajouter un Niveau {% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-center mb-4"> Ajouter un nouveau Niveau </h1>

    {{ form_start(form) }}

    <div class="mb-4">
        {{ form_label(form.titre, 'Titre', {'label_attr': {'class': 'font-bold text-gray-300'}}) }}
        {{ form_widget(form.titre, {'attr': {'class': 'form-input mt-1 block w-full text-black'}}) }}
        {{ form_errors(form.titre) }}
    </div>

    <br>
    
    <div class="mt-6">
        <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
            Enregistrer
        </button>
    </div>

    {{ form_end(form) }}
</div>
{% endblock %}
