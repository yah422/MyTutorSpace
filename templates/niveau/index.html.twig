{% extends 'base.html.twig' %}

{% block title %} Niveau {% endblock %}

{% block body %}

{# <div class="listNiveau">

    {% for niveau in niveaux %}
    <ul class="divide-y divide-gray-200 rounded-lg">
        <a href="{{ path('show_niveau',{'id': niveau.id })}}" class="text-black">
            <div class="niveauList">
                <li class="px-4 py-2">
                    <strong>{{ niveau.titre }}</strong>


                    {% if is_granted('ROLE_ADMIN') %}
                    <form action="{{ path('delete_niveau', {'id': niveau.id}) }}" method="post"
                        onsubmit="return confirm('Etes-vous sûre ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete_niveau') }}">
                        <button type="submit" class="text-black font-bold py-2 px-4 rounded"
                            style="background-color: #FFD700;">
                            Supprimer
                        </button>
                    </form>
                    {% endif %}

                </li>
            </div>
        </a>
    </ul>
    {% endfor %}
    <br>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('add_niveau') }}" class="text-black font-bold py-2 px-4 rounded"
        style="background-color: #FFD700;">
        Ajouter un niveau
    </a>
    {% endif %}

</div> #}
<div class="listNiveau">
{% for niveau in niveaux %}
<ul class="divide-y divide-gray-200 rounded-lg">
    <a href="{{ path('show_niveau', {'id': niveau.id }) }}" class="text-black">
        <div class="niveauList">
            <li class="px-4 py-2">
                <strong>{{ niveau.titre }}</strong>

                {% if is_granted('ROLE_ADMIN') %}
                <form action="{{ path('delete_niveau', {'id': niveau.id}) }}" method="post"
                    onsubmit="return confirm('Etes-vous sûre ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete_niveau') }}">
                    <button type="submit" class="text-black font-bold py-2 px-4 rounded"
                        style="background-color: #FFD700;">
                        Supprimer
                    </button>
                </form>
                {% endif %}
            </li>
        </div>
    </a>
</ul>
{% endfor %}
</div>
{% endblock %}