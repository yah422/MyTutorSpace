<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <link id="iconTitre" rel="icon" href="{{ asset('img/iconTitle.png') }}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Envoyer un message - MyTutorSpace</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body style="background-color: #1B4255;" class="text-gray-300 min-h-screen flex items-center justify-center">
    <div style="background-color: #567C8D;" class="container mx-auto max-w-5xl p-6 rounded-lg shadow-lg">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Section gauche avec illustration -->
            <div class="flex flex-col items-center justify-center">
                <img src="{{ asset('img/messages.svg') }}" alt="Messagerie Illustration" class="w-3/4 mb-4">
                <h1 style="color: #FFD700;" class="text-3xl font-extrabold mb-2 text-center">Messagerie</h1>
                <p class="text-gray-200 text-lg text-center">
                    Envoyez des messages personnalisés à vos étudiants et collègues.
                </p>
            </div>

            <!-- Section droite avec formulaire -->
            <div style="background-color: #1B4255;" class="p-8 rounded-lg shadow-md">
                {% if error is defined %}
                <div class="error">
                    {% for err in error %}
                    <p>{{ err.message }}</p>
                    {% endfor %}
                </div>
                {% endif %}

                <h2 class="text-xl font-bold mb-6 text-center" style="color: #FFD700;">Envoyer un nouveau message</h2>

                {{ form_start(formMessage, { 'attr': { 'class': 'space-y-6' } }) }}
                <div>
                    <label for="receiver" class="block text-sm font-medium text-gray-300">Destinataire</label>
                    {{ form_widget(formMessage.receiver, { 'attr': { 'class': 'w-full p-3 bg-[#567C8D] text-black
                    rounded focus:ring-2 focus:ring-[#FFD700]' } }) }}
                </div>

                <div>
                    <label for="messageContent" class="block text-sm font-medium text-gray-300">Message</label>
                    {{ form_widget(formMessage.messageContent, { 'attr': { 'class': 'w-full p-3 bg-[#567C8D] text-black
                    rounded focus:ring-2 focus:ring-[#FFD700]' } }) }}
                </div>

                <button type="submit" style="background-color: #FFD700; color: #000000;"
                    class="w-full font-bold py-3 rounded hover:bg-yellow-500 transition">
                    Envoyer le Message
                </button>
                {{ form_end(formMessage) }}
            </div>
        </div>
    </div>
</body>

</html>