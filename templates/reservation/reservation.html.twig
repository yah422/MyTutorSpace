{% extends 'base.html.twig' %}

{% block title %} Réservation de Cours {% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto p-8 mt-12 bg-gradient-to-r from-teal-400 to-blue-500 rounded-2xl shadow-xl text-white">
    <h1 class="text-4xl font-extrabold text-center mb-8">📚 Réservez votre leçon</h1>

    {{ form_start(form, {'attr': {'id': 'appointment_form', 'novalidate': 'novalidate', 'class': 'space-y-6'}}) }}

    <!-- Nom -->
    <div>
        {{ form_row(form.nom, {'attr': {'placeholder': 'Entrez votre nom', 'class': 'w-full bg-white text-gray-700
        rounded-lg px-4 py-3 shadow-md focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <!-- Prénom -->
    <div>
        {{ form_row(form.prenom, {'attr': {'placeholder': 'Entrez votre prénom', 'class': 'w-full bg-white text-gray-700
        rounded-lg px-4 py-3 shadow-md focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <!-- Email -->
    <div>
        {{ form_row(form.email, {'attr': {'placeholder': 'Entrez votre email', 'class': 'w-full bg-white text-gray-700
        rounded-lg px-4 py-3 shadow-md focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <!-- Date -->
    <div>
        {{ form_row(form.dateDebut, {'attr': {'id': 'dateInput','class': 'w-full bg-white text-gray-700 rounded-lg px-4 py-3 shadow-md
        focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <div>
        {{ form_row(form.dateFin, {'attr': {'id': 'dateInput','class': 'w-full bg-white text-gray-700 rounded-lg px-4 py-3 shadow-md
        focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <!-- Message -->
    <div>
        {{ form_row(form.message, {
        'attr': {
        'placeholder': 'Ajoutez un message pour votre tuteur...',
        'class': 'w-full bg-white text-gray-700 rounded-lg px-4 py-3 shadow-md h-28 focus:outline-none focus:ring-4
        focus:ring-teal-300'
        }
        }) }}
    </div>

    <!-- Créneaux horaires disponibles -->
    <div id="available-rdv" class="p-4 bg-white bg-opacity-90 rounded-lg text-gray-800 shadow-md">
        <h2 class="text-lg font-semibold mb-3">Créneaux horaires disponibles</h2>
        <div id="available-rdv"></div>

    </div>

    <!-- Cours -->
    <div>
        <h2 class="text-lg font-semibold mb-3">Choisissez un cours :</h2>
        {{ form_row(form.lecons, {'attr': {'class': 'w-full bg-white text-gray-700 rounded-lg px-4 py-3 shadow-md
        focus:outline-none focus:ring-4 focus:ring-teal-300'}}) }}
    </div>

    <!-- Bouton -->
    <div class="text-center mt-8">
        <button type="submit" id="appointment_save"
            class="w-full sm:w-auto bg-white text-teal-500 font-bold py-3 px-6 rounded-lg shadow-lg hover:bg-gray-100 hover:text-teal-600 transform hover:scale-105 transition-transform duration-200 focus:outline-none focus:ring-4 focus:ring-blue-300">
            🚀 Réserver maintenant
        </button>
    </div>

    {{ form_end(form) }}
</div>
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<script src="{{ asset('path/to/your/js/reservation.js') }}"></script>
{% endblock %}