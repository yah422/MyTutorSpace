<div class="max-w-md mx-auto p-6 bg-white border border-gray-200 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold text-gray-800 mb-4 text-center">
        Activation de votre compte sur <span class="text-blue-500">MyTutorSpace</span>
    </h1>
    <p class="text-gray-700 mb-4">
        Bonjour <span class="font-semibold">{{ user.prenom }}</span>,
    </p>
    <p class="text-gray-700 mb-4">
        Pour activer votre compte sur <span class="font-semibold">MyTutorSpace</span>, cliquez sur le lien ci-dessous.
    </p>
    <p class="mb-6">
        <a href="{{ absolute_url(path('verify_user', {token: token})) }}"
            class="inline-block px-4 py-2 text-white bg-blue-500 hover:bg-blue-600 rounded-lg shadow-md">
            Activer mon compte
        </a>
    </p>
    <p class="text-sm text-gray-500 mb-4 break-words">
        Si le lien ci-dessus ne fonctionne pas, utilisez ce token : <span class="font-mono text-gray-800">{{ token
            }}</span>
    </p>
    <p class="text-gray-700">
        Merci,
        <br>
        L'équipe <span class="font-semibold">MyTutorSpace</span>
    </p>
</div>