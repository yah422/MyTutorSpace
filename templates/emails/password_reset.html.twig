<div class="max-w-md mx-auto p-6 bg-white border border-gray-200 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold text-gray-800 mb-4 text-center">
        Réinitialisation de votre mot de passe
    </h1>
    <p class="text-gray-700 mb-4">
        Bonjour <span class="font-semibold">{{ user.prenom }}</span>,
    </p>
    <p class="text-gray-700 mb-4">
        Pour votre demande de réinitialisation de mot de passe, veuillez cliquer sur le lien ci-dessous.
    </p>
    <p class="mb-6">
        <a href="{{ url|raw }}"
            class="inline-block px-4 py-2 text-white bg-red-500 hover:bg-red-600 rounded-lg shadow-md">
            Réinitialiser mon mot de passe
        </a>
    </p>
    <p class="text-sm text-gray-500 break-words">
        Si le lien ci-dessus ne fonctionne pas, utilisez cette URL :
        <a href="{{ url|raw }}" class="text-blue-500 underline">{{ url|raw }}</a>
    </p>
    <p class="text-gray-700 mt-4">
        Merci,
        <br>
        L'équipe <span class="font-semibold">MyTutorSpace</span>
    </p>
</div>