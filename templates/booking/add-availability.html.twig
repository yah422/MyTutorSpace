{% extends 'base.html.twig' %}

{% block body %}
<h1>Add Tutor Availability</h1>

{{ form_start(form) }}
{{ form_row(form.startTime) }}
{{ form_row(form.endTime) }}
<button type="submit" class="btn btn-primary">Save</button>
{{ form_end(form) }}

<a href="{{ path('app_booking') }}" class="btn btn-secondary">Back to Availability List</a>
{% endblock %}