{% extends 'base.html.twig' %}

{% block title %}Réservation réussie{% endblock %}

{% block body %}
<div class="flex flex-col items-center justify-center min-h-screen">
    <div class="bg-white shadow-md rounded-lg p-8 w-full max-w-md">
        <h1 class="text-2xl font-bold text-green-600 text-center">🎉 Réservation réussie !</h1>
        <p class="text-center text-gray-600 mt-4">
            Merci d'avoir réservé. Nous avons bien enregistré votre réservation.
        </p>
        
        {# Détails de la réservation #}
        <div class="mt-6 text-center">
            <h3 class="text-lg font-medium text-gray-900">Détails de la réservation:</h3>
            <ul class="list-disc pl-5 text-gray-600 mt-2">
                <li><strong>Tuteur:</strong> {{ booking.tuteur.nom }} {{ booking.tuteur.prenom }}</li>
                <li><strong>Créneau sélectionné:</strong> {{ booking.preferredTimeSlot }}</li>
            </ul>
        </div>

        {# Lien retour à la page d'accueil #}
        <div class="mt-6 flex justify-center">
            <a href="{{ path('home') }}" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">
                Retour à l'accueil
            </a>
        </div>
    </div>
</div>
{% endblock %}
