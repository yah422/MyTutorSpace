{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
{% endblock %}

{% block body %}

<div class="container mx-auto px-4 py-8">

    <div style="margin: 0px; height:100vh; justify-content: flex-start;" id="homePage1Part"
        class="h-screen px-4 py-8 flex flex-col ">
        <div class="PicTitle flex flex-col md:flex-row items-center justify-between">
            <div class="TextTitle flex flex-col flex-start items-start text-left w-1/2">
                <h1 class="flex items-start justify-start content-start text-5xl font-bold mb-4">My Tutor
                    Space</h1>
                <p class="pHomePage">Donner aux tuteurs privés les outils pour façonner l'avenir, une leçon à la fois.
                </p>
                <br>
                <!-- Barre de recherche -->
                <form action="{{ path('app_search') }}" method="GET" class="relative">
                    <input type="text" name="q"
                        class="bg-white text-blue-900 placeholder-blue-900 px-4 py-2 rounded-full focus:outline-none w-64"
                        placeholder="Rechercher ..." value="{{ app.request.query.get('q') }}" />
                    <button type="submit" class="absolute right-0 top-0 mt-2 mr-4 text-blue-900">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor"
                            class="w-5 h-5">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M21 21l-4.35-4.35m2.55-6.15a7.5 7.5 0 11-15 0 7.5 7.5 0 0115 0z" />
                        </svg>
                    </button>
                </form>
            </div>
            <div class="w-1/2">
                <img src="/img/photoelearning1.svg" alt="photoElearning" class="max-w-full h-auto">
            </div>
        </div>
    </div>

    <!-- Section Choisir Une Matière -->
    <div style="margin-top: 30px; margin-bottom:30px;" class="text-center mb-8">
        <h2 style="color: #70F9D9;" class="text-4xl font-bold">Choisi Une Matière</h1>
            <div class="flex flex-wrap justify-center gap-3 mt-4">
                <!-- Boucle pour générer les boutons de matières -->
                {% for matiere in matieres %}
                <a href="{{ path('show_matiere', { 'id': matiere.id }) }}">
                    <button style="background-color: #ECECEC;"
                        class="text-black border border-gray-300 px-4 py-2 rounded hover:bg-gray-100 transition">
                        {{ matiere.nom ?? 'Nom non défini' }}
                    </button>
                </a>
                {% endfor %}
                <a href="{{ path('app_matiere') }}">
                    <button style="background-color:#70F9D9"
                        class="text-black border px-4 py-2 rounded hover:bg-white transition">
                        Toutes les matières
                    </button>
                </a>
            </div>
    </div>
    <br>
    <!-- Section Simple et Efficace -->
    <div class="text-center mb-10 w-full">
        <h2 class="text-5xl font-bold text-[#FFD700]">Simple et Efficace</h2>
        <br>
        <!-- Wrapper centré pour les cartes -->
        <div class="flex flex-wrap justify-center gap-6 mt-8">

            <!-- Première carte -->
            <div style="background-color: #FFD700;"
                class="text-black p-6 py-10 rounded-lg shadow-lg w-1/4 sm:w-1/4 text-center flex flex-col items-center">
                <img src="/img/objectif.png" alt="Icône cible" class="mb-4">
                <h3 class="text-2xl font-semibold mb-4">Fixe tes objectifs</h3>
                <p>Donnez aux cours privés une direction, un objectif et un but pour réussir.</p>
            </div>

            <!-- Deuxième carte -->
            <div style="background-color: #FFD700;"
                class="text-black p-6 py-10 rounded-lg shadow-lg w-1/4 sm:w-1/4 text-center flex flex-col items-center">
                <img src="/img/search.png" alt="icône loupe" class="mb-4">
                <h3 class="text-2xl font-semibold mb-4">Trouve ton tuteur</h3>
                <p>Donnez aux tuteurs privés toutes les informations pour vous aider à réussir.</p>
            </div>

            <!-- Troisième carte -->
            <div style="background-color: #FFD700;"
                class="text-black p-6 py-10 rounded-lg shadow-lg w-1/4 sm:w-1/4 text-center flex flex-col items-center">
                <img src="/img/learn.png" alt="icône livre" class="mb-4">
                <h3 class="text-2xl font-semibold mb-4">Commence à apprendre</h3>
                <p>Donnez aux tuteurs privés les clés pour s’adapter et répondre à vos besoins.</p>
            </div>

        </div>
    </div>

    <!-- Section Blocs d'Information avec Images -->
    <div class="space-y-12">
        {% for block in [
        {'title': 'Fixe tes objectifs', 'text': 'Lorem ipsum dolor sit amet...', 'img': '/img/education.svg'},
        {'title': 'Atteins tes buts', 'text': 'Consectetur adipiscing elit...', 'img': '/img/goal.svg'},
        {'title': 'Trouvez les meilleurs tuteurs', 'text': 'Sed do eiusmod tempor incididunt...', 'img':
        '/img/tutors.svg'},
        {'title': 'Tarifs compétitifs', 'text': 'Ut labore et dolore magna aliqua...', 'img': '/img/price.svg'}
        ] %}
        <div class="flex flex-col {{ loop.index is odd ? 'md:flex-row' : 'md:flex-row-reverse' }} items-center gap-6">
            <!-- Texte -->
            <div class="text-white md:w-1/2">
                <h3 class="text-2xl font-bold">{{ block.title }}</h3>
                <p class="mt-4">{{ block.text }}</p>
            </div>
            <!-- Image -->
            <div class="md:w-1/2">
                <img src="{{ block.img }}" alt="{{ block.title }}" class="w-full rounded-lg shadow-lg">
            </div>
        </div>
        {% endfor %}
    </div>

</div>

{% endblock %}