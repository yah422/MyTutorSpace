<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <link id="iconTitre" rel="icon" href="{{ asset('img/iconTitle.png') }}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Se connecter à MyTutorSpace</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body style="background-color: #1B4255;" class="text-gray-300 min-h-screen flex items-center justify-center">
    <div style="background-color: #567C8D;" class="container mx-auto max-w-5xl p-6 rounded-lg shadow-lg">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Section gauche avec illustration -->
            <div class="flex flex-col items-center justify-center">
                <img src="{{ asset('img/loginPic.svg') }}" alt="Connexion Illustration" class="w-3/4 mb-4">
                <h1 style="color: #FFD700;" class="text-3xl font-extrabold mb-2 text-center">Bienvenue sur MyTutorSpace
                </h1>
                <p class="text-gray-200 text-lg text-center">
                    Connectez-vous pour accéder à votre espace et poursuivre votre parcours.
                </p>
            </div>

            <!-- Section droite avec formulaire -->
            <div style="background-color: #1B4255;" class="p-8 rounded-lg shadow-md">
                {% if error %}
                <div class="bg-red-600 text-white p-4 rounded mb-4">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
                {% endif %}

                {% if app.user %}
                <div class="text-white p-3 rounded bg-green-600 mb-4">
                    Vous êtes connecté en tant que {{ app.user.userIdentifier }}.
                    <a href="{{ path('app_logout') }}" class="underline text-gray-300 hover:text-blue-300">Se
                        déconnecter</a>.
                </div>
                {% endif %}

                <form method="post" class="space-y-6">
                    <div>
                        <label for="username" class="block text-sm font-medium text-gray-300">Email</label>
                        <input type="email" id="username" name="_username" value="{{ last_username }}"
                            class="w-full p-3 bg-[#567C8D] text-black rounded focus:ring-2 focus:ring-[#FFD700]"
                            required autofocus>
                    </div>

                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-300">Mot de passe</label>
                        <input type="password" id="password" name="_password"
                            class="w-full p-3 bg-[#567C8D] text-black rounded focus:ring-2 focus:ring-[#FFD700]"
                            required>
                    </div>

                    <label>
                        <input type="checkbox" name="_remember_me"> Se souvenir de moi
                    </label>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                    <button type="submit" style="background-color: #FFD700; color: #1B4255;"
                        class="w-full font-bold py-3 rounded hover:bg-yellow-500 transition">
                        Se connecter
                    </button>
                </form>

                <p class="text-center text-sm mt-6">
                    Vous n'avez pas de compte ?
                    <a href="{{ path('app_role_choice') }}" style="color: #FFD700;"
                        class="hover:underline">Inscrivez-vous ici</a>.
                </p>
            </div>
        </div>
    </div>
</body>

</html>