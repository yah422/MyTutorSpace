<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demande de réinitialisation de mot de passe</title>
    <link id="iconTitre" rel="icon" href="{{ asset('img/iconTitle.png') }}">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body style="background-color: #1B4255;" class="text-gray-300 min-h-screen flex items-center justify-center">
    <div style="background-color: #567C8D;" class="container mx-auto max-w-5xl p-6 rounded-lg shadow-lg">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Section gauche avec illustration -->
            <div class="flex flex-col items-center justify-center">
                <img src="{{ asset('img/resetPassword.svg') }}" alt="Illustration Connexion" class="w-3/4 mb-4">
                <h1 style="color: #FFD700;" class="text-3xl font-extrabold mb-2 text-center">
                    Bienvenue sur MyTutorSpace
                </h1>
                <p class="text-gray-200 text-lg text-center">
                    Réinitialisez votre mot de passe et poursuivez votre parcours.
                </p>
            </div>

            <!-- Section droite avec formulaire -->
            <div style="background-color: #1B4255;" class="p-8 rounded-lg shadow-md">
                {% if error %}
                <div class="bg-red-600 text-white p-4 rounded mb-4">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
                {% endif %}

                {% if app.user %}
                <div class="bg-green-600 text-white p-3 rounded mb-4">
                    Vous êtes connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>.
                    <a href="{{ path('app_logout') }}" class="underline text-gray-300 hover:text-blue-300">
                        Se déconnecter
                    </a>.
                </div>
                {% endif %}

                {{ form_start(requestPassForm, { 'attr': { 'class': 'space-y-4' } }) }}
                {{ form_row(requestPassForm.email, { 'attr': { 'class': 'w-full text-black p-2 rounded border-gray-300' } }) }}
                <button type="submit"
                    class="w-full bg-yellow-400 text-black py-2 rounded hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-yellow-300">
                    Envoyer
                </button>
                {{ form_end(requestPassForm) }}
            </div>
        </div>
    </div>
</body>

</html>