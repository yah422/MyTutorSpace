<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %} Bienvenue {% endblock %}</title>
    <link id="iconTitre" rel="icon" href="{{ asset('img/iconTitle.png') }}">
    {% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
    <link href="node_modules/@fullcalendar/core/main.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.0/main.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.0/main.min.js"></script>

    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}


    <!-- Tarte au Citron Script -->
    <script src="https://cdn.jsdelivr.net/npm/tarteaucitronjs@1.9.3/tarteaucitron.js"></script>
    <script>
        tarteaucitron.init({
            "privacyUrl": "", // URL of your privacy policy
            "hashtag": "#tarteaucitron", // Open the panel with this hashtag
            "cookieName": "tarteaucitron", // Cookie name
            "orientation": "bottom", // Banner position (top - bottom - middle - popup)
            "groupServices": false, // Group services by category
            "showAlertSmall": false, // Show the small banner on bottom right
            "cookieslist": false, // Show the cookie list
            "showIcon": true, // Show cookie icon to manage cookies
            "iconPosition": "BottomRight", // Position of the icon between BottomRight, BottomLeft, TopRight and TopLeft
            "adblocker": false, // Show a Warning if an adblocker is detected
            "DenyAllCta": true, // Show the deny all button
            "AcceptAllCta": true, // Show the accept all button
            "highPrivacy": true, // HIGHLY RECOMMENDED Disable auto consent
            "handleBrowserDNTRequest": false, // If Do Not Track == 1, disallow all
            "removeCredit": false, // Remove credit link
            "moreInfoLink": true, // Show more info link
            "useExternalCss": false, // If false, the tarteaucitron.css file will be loaded
            "useExternalJs": false, // If false, the tarteaucitron.js file will be loaded
            "readmoreLink": "", // Change the default readmore link pointing to tarteaucitron.io
            "mandatory": true // Show a message about mandatory cookies
        });
    </script>
    <script>
        // Google Analytics
        tarteaucitron.user.gtagUa = '694190220787-elursji5hbf1qjjrtgg9g9mdhm11c68a.apps.googleusercontent.com';
        tarteaucitron.user.gtagMore = function () { /* add optionnal gtag() */ };
        (tarteaucitron.job = tarteaucitron.job || []).push('gtag');

        // Google Sign-In
        (tarteaucitron.job = tarteaucitron.job || []).push('googlesignin');
    </script>
</head>

<body>
    <header>
        <nav class="bg-[#1B4255] p-4">
            <div class="container mx-auto flex justify-between items-center">
                <!-- Logo de la navbar -->
                <a href="{{ path('home') }}" style="color:#FFD700;"
                    class="text-2xl flex flex-rowtext-xl font-bold items-center">
                    <img id="iconTitre" src="{{ asset('img/iconTitle.png') }}" loading="lazy" alt="Logo">
                    <span> MyTutorSpace </span>
                </a>

                <!-- Menu principal -->
                <ul class="flex items-center space-x-6">
                    <!-- Autres liens -->
                    <li><a href="{{ path('app_lecon') }}" class="text-gray-300 hover:text-white">📚 Cours</a></li>
                    <li><a href="{{ path('app_user') }}" class="text-gray-300 hover:text-white">🔍 Trouve un Tuteur</a>
                    <li><a href="{{ path('app_forum') }}" class="text-gray-300 hover:text-white">💭 Forum</a>
                    </li>

                    <!-- Vérifier si l'utilisateur est connecté -->
                    {% if app.user %}
                    <li>
                        <a class="hover:text-white text-gray-300"
                            href="{{ path('app_show_user', { 'id': app.user.id }) }}">
                            <div class="profilNavBar">
                                {# <div>
                                    <img src="{{ asset('img/user.png') }}" loading="lazy" alt="iconProfil">
                                </div> #}
                                <div class="pProfil">
                                    <p>👤 {{ app.user.prenom }}</p>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_logout') }}" style="background-color: #FFD700;"
                            class="bg-500 text-black px-4 py-2 rounded hover:bg-700">
                            <strong>Se déconnecter</strong>
                        </a>
                    </li>
                    {% else %}
                    <li>
                        <a href="{{ path('app_login') }}" style="background-color: #ECECEC; color: black;"
                            class="bg-500 text-gray-300 px-4 py-2 rounded hover:bg-700">
                            <strong>Se connecter</strong>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_role_choice') }}" style="background-color: #FFD700; color: black;"
                            class="bg-500 text-gray-300 px-4 py-2 rounded hover:bg-700">
                            <strong>S'inscrire</strong>
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
    </header>

    <main>
        {% block body %}
        {% endblock %}
    </main>

    <footer class="bg-[#1B4255] text-white py-8">
        <div class="container mx-auto grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Section Informations -->
            <div>
                <h3 class="text-lg font-semibold mb-4">À propos</h3>
                <p class="text-gray-300 text-sm leading-relaxed">
                    MyTutorSpace est une plateforme dédiée aux tuteurs et étudiants pour un apprentissage personnalisé
                    et efficace.
                </p>
            </div>

            <!-- Section Liens utiles -->
            <div>
                <h3 class="text-lg font-semibold mb-4">Liens utiles</h3>
                <ul class="space-y-2">
                    <li>
                        <a href="{{ path('app_contact') }}"
                            class="text-gray-300 hover:text-yellow-400 flex items-center">
                            📞 <span class="ml-2">Contact</span>
                        </a>
                    </li>
                    {# Ajouter ou désactiver des liens ici si nécessaire #}
                </ul>
            </div>

            <!-- Section Réseaux sociaux -->
            <div>
                <h3 class="text-lg font-semibold mb-4">Suivez-nous</h3>
                <div class="flex flex-col space-y-2">
                    <a href="https://facebook.com" target="_blank" class="hover:text-yellow-400 flex items-center">
                        <i class="fab fa-facebook fa-lg"></i>
                        <span class="ml-2">Facebook</span>
                    </a>
                    <a href="https://twitter.com" target="_blank" class="hover:text-yellow-400 flex items-center">
                        <i class="fab fa-twitter fa-lg"></i>
                        <span class="ml-2">Twitter</span>
                    </a>
                    <a href="https://instagram.com" target="_blank" class="hover:text-yellow-400 flex items-center">
                        <i class="fab fa-instagram fa-lg"></i>
                        <span class="ml-2">Instagram</span>
                    </a>
                </div>
            </div>
        </div>
        <div class="text-center text-sm text-gray-300 mt-8">
            &copy; {{ "now"|date("Y") }} MyTutorSpace. Tous droits réservés.
        </div>
    </footer>

    <script src="{{ asset('js/leconIndex.js') }}"></script>
    <script src="{{ asset('js/reservation.js') }}"></script>
    <script src="node_modules/@fullcalendar/core/main.js"></script>
    <script src="node_modules/@fullcalendar/daygrid/main.js"></script>
    <script src="node_modules/@fullcalendar/timegrid/main.js"></script>
    <script src="node_modules/@fullcalendar/interaction/main.js"></script>
</body>

</html>